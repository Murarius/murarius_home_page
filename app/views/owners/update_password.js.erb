<% if @good_password %>
  $('#dialog').dialog('close');
  $('#flash-main').html("<%= escape_javascript(render 'shared/flash_messages' ) %>");
<% else %>
  $('#flash-main').html("<%= escape_javascript(render 'shared/flash_messages' ) %>");
  $('#owner_password').val('');
  $('#owner_password_confirmation').val('');
<% end %>

<% flash.clear %>
